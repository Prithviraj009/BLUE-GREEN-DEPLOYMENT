apiVersion: v1
kind: Service
metadata:
  name: service-bg-preview
spec:
  type: ClusterIP
  selector:
    app: bg-app
    version: blue
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080